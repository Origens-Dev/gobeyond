{
  "name": "@go-beyond/edge-middleware",
  "version": "0.0.0-stream4-stub",
  "private": true,
  "description": "Stream 4 stub: public User Worker edge-middleware contract for Workers for Platforms (ADR 013). Not published until contract ships; pin a tagged release from gobeyond-internal.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Origens-Dev/gobeyond.git",
    "directory": "packages/edge-middleware"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
